# 文明 VI 群控 Mod：实现前自检报告

日期：2026-09-20。范围：读取方案、核对本机原生脚本、检索真实 UI Mod 的加载声明。本轮没有编写 Mod 程序、生成安装包、安装 Mod、修改游戏配置或启动游戏测试。以下是风险审查与验收设计，不是新 Mod 已通过运行测试的报告。

用户重点问题：过去的 Mod 可以安装、可以上传 Steam 创意工坊，但进入对局地图后完全没有界面或效果。旧包已删除，目前不能确定其实际失败原因。

## 已核实的重要加载细节

本机 `C:\steam\steamapps\common\Sid Meier's Civilization VI\DLC\Expansion2\UI\Replacements\InGame.lua` 第 348–353 行：

- 原生游戏枚举 `Modding.GetUserInterfaces("InGame")`。
- 在加载界面前打印 `Loading InGame UI - ` 加上下文路径。
- 设置 `isHidden = true`，再调用 `ContextPtr:LoadNewContext(...)` 创建 Mod 界面。

由此确认：界面被注册、被枚举、甚至 Lua 开始运行，均不等于界面已经显示。Mod 必须在正确生命周期内管理自己的可见状态，并检查父级、布局与弹窗遮挡。这是本次发现的具体高优先级风险，不能据此断言旧 Mod 就是因它失败。

本机 `DLC\Expansion2\Expansion2.modinfo` 第 302–315 行有 `AddUserInterfaces`、`Context=InGame` 和 XML 入口；第 492–497 行通过 `ReplaceUIScript` 替换 WorldInput。`DLC\Expansion2\UI\Replacements\InGame.xml` 第 9 行注明界面顺序影响输入优先级和绘制顺序，第 139–140 行定义新增界面的父容器。

## A. 游戏内看不见/没有反应的风险

| 编号 | 潜在问题 | 处理与验证 |
| --- | --- | --- |
| A1 | Mod 被发现，但界面没有注册到当前对局 | 核对最终安装包的 InGameActions / AddUserInterfaces / Context=InGame / XML 入口；不能只看工程文件、文件清单或上传结果。检查原生的 Loading InGame UI 日志。 |
| A2 | 上下文按隐藏状态创建，之后从未显示 | 设计明确的加载、就绪、显示、隐藏和恢复流程；验证进入地图、关闭弹窗、读档后实际看得到面板。显示日志本身不能代替屏幕证据。 |
| A3 | XML/Lua 名称、路径、资源或控件 ID 不一致 | 检查安装包真实文件；同名 XML/Lua 配对，辅助文件按需导入；先用原生基础控件与固定文字验证入口。避免通用文件名和上下文 ID 冲突。 |
| A4 | UI 代码放进 GameplayScript 或前端上下文 | 分清元数据、前端、UI 与玩法脚本；界面和操作请求先在 UI 上下文验证。后台脚本打印成功不能证明界面入口成功。 |
| A5 | 顶层 include、语法或首个 API 调用失败，导致初始化中止 | 入口保持极小依赖；详细记录错误与阶段。发生语法错误时，连第一条自定义日志也可能没有，因此还要检查原生 Lua/XML 加载错误。不能用静默异常处理把报错藏掉。 |
| A6 | 玩家、地图、控件还没就绪，初始化就永久返回 | 面板壳与玩法就绪分离；允许显示“等待对局就绪”。即时检查和生命周期事件配合，初始化只完成一次，不能只等一个可能已经触发过的事件。 |
| A7 | 拥有扩展包，但本局规则集不匹配组件条件 | 记录本局实际规则集和 GameCore，不以 DLC 是否安装判断；检查条件是否满足。首个显示探针尽量不引入不必要的条件和玩法依赖。 |
| A8 | 本地、旧 Documents、AppData、创意工坊存在同 ID 副本，实际加载了旧文件 | 检查这几处的本 Mod 副本；稳定 Mod ID 加不同构建标识，屏幕与日志显示同一标识；确认安装包路径。不能通过不断换 ID 或批量清空其他 Mod 解决。 |
| A9 | 界面确实存在，但尺寸为零、屏幕外、透明、父级隐藏或被遮挡 | 用固定位置、可读文字和标准控件建立基线；检查父级、大小、缩放、分辨率与弹窗恢复。界面隐藏时暂停输入，不能强行覆盖外交/菜单。 |
| A10 | 面板显示，但地图输入被原生界面、单位旗帜、城市界面或其他 Mod 拦截 | 面板点击和地图输入分别计数；验证空地、单位旗帜、城市、战略视图。独立上下文的处理器不能被当作全局输入钩子。 |
| A11 | 扩展包/其他 UI Mod 替换 WorldInput，自己的接入未生效 | 保留本机 Expansion2 的继承链；适配器有独立版本标识和握手。需要实际兼容适配，单纯提高 LoadOrder 不会合并多个替换。 |
| A12 | 只测试旧存档，或重载仍使用旧上下文/旧文件列表 | 首次用新对局建立基线；入口或新增文件改变后完整重启；随后测试存读档。AffectsSavedGames 等属性不是兼容性证明。 |

优先顺序：既然用户过去能在附加内容中识别包，应先检查 A1–A7 的游戏内入口与初始化链路；目录 A8 仍需核实，但不能把它当作唯一解释。

## B. 接口验证边界

| 能力 | 当前证据 | 尚须实测 |
| --- | --- | --- |
| 新增地图内界面 | 本机官方 DLC 声明与 InGame 加载器 | 本 Mod 的 XML/Lua 配对、可见生命周期、控件点击 |
| 地图鼠标输入 | WorldInput 有按下、移动、松开、双击路由 | 新 Mod 接入后的顺序、旗帜处理、事件消费、不重复触发原生命令 |
| 单位移动/攻击 | Civ6Common、WorldInput、UnitFlagManager 中存在对应原生请求 | 非当前选中单位、参数语义、拒绝条件、护送影响、取消行为 |
| 路径/未来攻击位预测 | 已发现部分原生路径和操作判定用法 | 不能假定当前地点的 CanStartOperation 支持假想地点；逐项验证需要的数据和签名 |
| 屏幕框选与预览 | 有地图输入与世界锚点相关代码 | 投影、战略视图、缩放和地图环绕边界；不能虚构 WorldToScreen 等函数 |
| 执行完成与取消 | UnitPanel 监听移动与操作事件 | 事件顺序、重复/缺失、快速动画、取消在途路径、玩家接管后的命令归属 |

跨 UI 上下文不能靠随意设置同名全局函数来假定 WorldInput 已改动。明确通过原生支持并经验证的事件或接口交换普通数据，不长期共享失效单位对象。记录接口存在性与其调用上下文，但“函数存在”也不能代替行为测试。

## C. 功能层新增或需收紧的风险

| 编号 | 潜在问题 | 处理与验证 |
| --- | --- | --- |
| C1 | 右键既触发群控又触发原生单兵命令 | 明确输入消费；每次用户操作有一个批次，防止重复提交。只在本模式实际接管事件时拦截。 |
| C2 | 双击先触发单击，两次选择互相清空；从图标起拖与空地起拖不同 | 给单击/双击/拖动设置明确优先级和拖动阈值；测试从旗帜起拖、反向框选、叠放单位。 |
| C3 | 松开鼠标发生在窗口外、Alt-Tab 或弹窗中，框选状态卡住 | 失焦、取消、模式切换、回合结束时清空拖动状态和占位预约。原生 WorldInput 也有类似状态清理。 |
| C4 | 只等战斗动画结束，关闭动画后队列永久等待 | 根据实际单位/目标/操作状态推进，事件只触发复核；设超时并报告原因。不能把每个事件等同于一整条命令完成。 |
| C5 | 超时后重发，但前一条已执行，造成二次移动或攻击 | 超时先复核状态，结果不确定则暂停；不盲目重试。批次版本与已处理步骤去重，旧回调不能推进新批次。 |
| C6 | 调用普通右键辅助函数，意外变成攻击、换位、劫掠或宣战 | 普通集结与攻击分别设计，核对所用原生函数的完整分支和参数；不把方便的综合右键函数当成纯移动。 |
| C7 | “可以发动操作”被误当作一定能攻击 | 本机 WorldInput 注释指出部分判定在零移动力时仍可能返回可攻击，需要结合移动力检查；再结合实际攻击次数、能力和执行结果。 |
| C8 | 终点不冲突，但途中单位互堵或形成循环等待 | 加入中途停留位置与释放顺序；有限重排；无解时待命，不默认调走未选单位或拆掉护送。 |
| C9 | 单位升级、合并、死亡或易主后，列表仍指向旧对象 | 每步重新取得并验证单位；重建集合与占位预约。原生自动切换选中单位不能误改群控集合。 |
| C10 | 攻击目标死亡后仍残留原生多回合路径；取消误伤玩家新命令 | 原生在途动作数量受控；取消与批次归属绑定。无法确定命令归属时暂停，不强行覆盖玩家操作。 |
| C11 | AI 回合、自动结束回合、玩家切换时继续下令 | 每次提交检查本地玩家、控制权与活动回合；回合结束暂停；临时选择/队列不能混到另一局。 |
| C12 | 存读档重复注册监听，一次点击执行两遍 | 初始化可重复调用但只生效一次；退出清理；读档默认不自动重放尚未确认的攻击队列。持久化状态带对局标识和格式版本。 |
| C13 | 使用全地图真实数据规划，泄露迷雾敌人；新敌人出现导致原路失效 | 敌方目标与路线评估只用玩家允许知道的信息；途中揭示阻碍即重新检查/暂停。 |
| C14 | 对全部单位做全图寻路，后期卡顿被误认为没反应 | 限定候选范围、分帧规划、进度/取消按钮、同一状态缓存；地图变化后失效缓存；30/100 单位记录实测耗时。 |
| C15 | 自定义兵种、宗教、空军等被粗略归类，产生非法叠放/攻击 | 使用当前对局合并后的数据；普通 FormationClass 只是初筛，特殊机制逐项适配；不支持时明确提示。 |

本轮不推翻此前关于落点分配、混合占位、射程、目标消失的规则；上述项目补充它们所依赖的输入、生命周期与异步执行条件。

## D. 实施时的首要验收关卡（本轮不实现）

1. **原生发现入口**：在明确的新对局中，找到针对本 Mod 的 `Loading InGame UI - ...`。这行出现在 LoadNewContext 调用前，只证明游戏准备加载它，不证明 XML/Lua 已成功。
2. **XML 与 Lua 启动**：结合原生错误日志和本 Mod 启动标识，确认对应文件和正确构建号开始运行。
3. **实际显示与按钮**：屏幕上确实出现固定版本面板；点击自检按钮计数变化。没有选中单位、没轮到玩家行动时，面板仍应说明当前状态，而不是完全消失。
4. **生命周期恢复**：完整重启、存读档、进入/退出城市和外交界面后仍正确显示，输入不遮挡其他界面。
5. **地图输入**：分别验证鼠标按下、拖动、松开、双击、右键；同时确认原生操作没有被重复执行或永久拦截。
6. **单位命令**：一个当前选中单位、一个非当前选中单位，再到三个单位。记录“检查通过/拒绝”“已提交”“实际完成/失败/暂停”，不能把三者混成一个成功状态。

故障定位顺序：当前日志与构建号 → 游戏内入口枚举 → XML/Lua 启动 → 请求显示与真实可见性 → 按钮 → 地图输入 → 单位命令。每一层有独立证据，停止在第一处断点，先修复再扩展。

当正式开始开发时，第一个包只承担最小显示、自检与诊断；不能让复杂的寻路、兵种解析或接口调用决定基础面板是否能出现。需要帮助模块失败时，保留诊断入口并展示错误；连入口都失败时，回到原生日志定位，不能承诺面板能报告自身所有加载错误。

## 参考

- 本机官方 `DLC\Expansion2\UI\Replacements\InGame.lua`、`InGame.xml`、`DLC\Expansion2\Expansion2.modinfo`、`Base\Assets\UI\WorldInput.lua`、`Civ6Common.lua`、`Panels\UnitPanel.lua`。
- 真实 UI Mod 的入口声明：<https://github.com/EilRoviSoft/Civ6-CQUI-Lite/blob/main/CQUI.modinfo>。
- 作者的最小 Lua Mod 示例：<https://jonathanturnock.github.io/civ-vi-modding/docs/hello-world-lua-mod/>。此示例是 GameplayScript 日志验证，不能拿来代替本项目的可见 UI 入口。
