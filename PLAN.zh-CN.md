# 文明 VI 批量单位控制 Mod：实现与验收方案

## p2.7：残余移动力自动等待

使用原生SKIP_TURN避免仍受群控管理但没有合法继续落点的单位反复要求命令，保留行军/进攻目标。到达、手动释放和原生拒绝分别处理；本轮状态见HANDOFF，实测步骤见TEST-p2.7。

## p2.6：闲置自动交战与协同推进

按用户最新要求加入可关闭自动交战：原生预估优势/近均势且预计存活才攻击，劣势查找更安全可达格，否则待命；手动和已有任务优先。明确进攻命令跨回合跟随同一可见敌军，使用可达前沿约束、稀缺落点优先、匹配落点执行与一次让位后重查。未知动作不重试。修复存档清空传输，迁移新格式避免旧残留。实现和测试状态以HANDOFF顶部为准；旧版“仅本回合攻击”的限制已由本轮授权更新，协同进攻跨读档仍未实现。

## p2.5：清晰控制台与连续操作

本轮按用户要求重整三段控制台与分层目录。父类浏览和应用分开，侦察独立于近战，支援集中归类；不扩展双击到全地图。关闭群控与清除选择仅改变选择状态，停止按钮管理已执行命令。新增游戏内属性保存行军，读取后逐单位验证，未确认的在途命令不重放。p2.4接敌、跨回合全员到达已实测通过；p2.5新界面与读档通路待实测。最新交接见HANDOFF顶部；以下保留历史设计。

## p2.4：按用户实测调整默认敌军指令

用户认为“远处战斗单位不动，必须手动移到攻击位”不符合群体操控。因此敌軍目标的确认执行改为明确显示“推进与攻击”：能立即攻击的优先，其他陆地战斗单位在本回合合法范围内接敌，到位后再问原生引擎能否攻击，未能攻击者停步，不虚报已攻击。混选平民不影响战斗单位安排。原先区分“只攻击”和“推进”是历史设计，此次用户反馈授权默认包含本回合推进。

目标死亡、位移、失去视野停止，不自行跨回合追击。尚未完成海空接敌、护送、城市攻击。双击屏幕内同兵种已实机通过。p2.3行军事件误判修复后，跨回合仍须实机复验，不能只以模拟测试判通过。

## p2.3 行军与双击接续（优先于历史阶段限制）

用户现已要求普通行军自动跨回合，并能向迷雾目标行进。实现以原生路径成本分配当回合中间停留格，并保留最终目标；回合结束保留任务，回合开始重查，不要求重新选取。地图探索只使用玩家允许看到的信息；路径/地形阻塞后重查，不自动攻击或宣战。UI会话内保留行军，读档持久化仍待后续实施和验证。

双击同兵种已在选取模块实现，默认只选屏幕内同 UnitType，Shift追加；本轮补齐离线回归。用户不需要等待行军完善才能使用双击。全图同类仍应作为未来明确操作，不把双击扩成全图选择。

## 2026-09-21 最新补充（优先于下方历史状态）

p2.1 已完成混合/同类单位集结、零移动力待命及三至四单位批次的实机验证。本轮按用户要求开发 p2.2：分层筛选全部单位、陆海空战斗细类、医疗等支援、建造者/开拓者、其他非战斗单位、九类伟人；已交战敌人格默认进入攻击预览，友军占用格在周边分配合法集结点。

攻击先实现当前位置合法攻击（远程优先、相邻近战随后），目标身份/位置/视野/外交变化即停。移动后补打、城市攻击和跨回合推进仍是后续阶段，不能将本次攻击原型描述成最终完整版。最新安装和测试状态见 HANDOFF.zh-CN.md。

日期：2026-09-21。当前状态：已安装 p1.3 框选测试版；加载、框选、旗帜入口已完成部分实测。工作区已有未安装、未验收的集结原型。因用户指出拖地图冲突，原 P3 选取工作提前进行；原 P1 单兵命令、P2 群体移动仍须验收，不能按构建号推定完成。最新进度见 HANDOFF.zh-CN.md。

实现前审查见 [AUDIT.zh-CN.md](./AUDIT.zh-CN.md)。其中补充了原生 InGame 加载器以隐藏状态创建 Mod 界面的证据，并将“入口被发现、Lua 启动、实际显示、地图输入、单位命令”分为独立验收关卡。此前仅规划的要求已由用户后续开始实施、恢复工作的指示更新。

## 目标与首版环境

实现用户描述的操作：框选一批己方单位，或双击选中同兵种，再对地图位置或敌方目标下达一次命令，由 Mod 分别安排单位执行。保留文明 VI 的回合制、行动力、地形、视野、外交、射程与占位规则。

用户已确认：Steam 版，启用了扩展包，之前尝试的 Mod 已删除，需要从头制作。首版暂按 Windows、风云变幻规则集、单人游戏设计；联机尚未验证，不能据 UI Mod 的形式推断联机一定可用。是否同时覆盖标准规则和迭起兴衰规则，在首版通过后安排。

## 已核实的本机依据

- 游戏安装目录：`C:\steam\steamapps\common\Sid Meier's Civilization VI`。
- Steam 清单记录 buildid：`15296837`。这是 Steam 构建号，不等同于游戏菜单中的版本号。
- 同时存在两个用户 Mods 目录：
  - `C:\Users\31767\Documents\My Games\Sid Meier's Civilization VI\Mods`
  - `C:\Users\31767\AppData\Local\Firaxis Games\Sid Meier's Civilization VI\Mods`
- 最近找到的 Lua.log、Modding.log、Database.log、UserInterface.log 位于 AppData 用户目录的 `Logs`，写入日期为 2026-09-18。
- 这些事实说明不能直接照旧教程假定安装与日志路径。哪个 Mods 目录被当前游戏实际扫描，仍须用唯一 ID 的加载探针确认。两个目录存在，不足以认定旧 Mod 失败原因。
- 本轮只读核对游戏目录与脚本，没有修改游戏安装文件、用户配置、存档或现有 Mod。

### 原生脚本证据

以下均已读取本机文件，可在实施时再次按当前版本定位：

1. `Base\Assets\UI\Civ6Common.lua`：`MoveUnitToPlot`、`RequestMoveOperation` 使用明确的单位对象；存在 `UnitManager.RequestOperation(..., UnitOperationTypes.MOVE_TO, ...)`、远程攻击及宣战检查。
2. `Base\Assets\UI\WorldInput.lua`：有左键按下/松开、鼠标移动、右键、双击消息路由；`Initialize` 中注册输入处理器。
3. `DLC\Expansion2\Expansion2.modinfo`：通过 `AddUserInterfaces` 和 `Context=InGame` 添加界面，通过 `ReplaceUIScript` 替换 `WorldInput`。
4. `DLC\Expansion2\UI\Replacements\WorldInput_Expansion2.lua`：继承扩展一的地图输入脚本。输入适配必须保留这一继承链。
5. `Base\Assets\UI\Panels\UnitPanel.lua`：监听 `UnitMoveComplete`、`UnitOperationDeactivated` 等事件，为命令状态跟踪提供参考。
6. `Base\Assets\UI\UnitFlagManager.lua`：单位旗帜也有独立的选取/攻击处理，不能只验证点击空白地图。

上述证据支持技术路线，但尚未证明框选、非当前选中单位下令、批量执行和旧存档兼容在新 Mod 中已经可用。

## 预期操作

首版提供明显的“群控模式”开关，进入后：

| 操作 | 预期行为 |
| --- | --- |
| 左键拖框 | 选中框内的己方单位；首版优先陆军战斗单位 |
| Shift + 点选/框选 | 增加或移除选择，显示选中数量 |
| 双击己方单位 | 默认选中当前屏幕内同单位类型的己方单位 |
| “全图同类”按钮 | 明确扩大到全图，避免意外调走驻军 |
| 右键空地 | 向目标及周边合法空位集结 |
| 右键已交战目标 | 对仍合法的同一敌方目标依次发动攻击 |
| Esc / 清除按钮 | 退出待下令状态或清除选择；提供独立停止队列操作 |

键位先检查与游戏现有绑定的冲突，再确定默认值。数字编队是后续便利功能。这里的编队保留每个单位，不改变游戏中的军团/集团军机制。

### 左键拖地图与框选的冲突（用户补充）

- 普通模式：保留原生左键拖地图。群控模式：左键拖动框选，模式开关持续显示状态。首版使用可见按钮，快捷键待核对绑定后确定。
- 群控模式内需要拖地图时，退出群控模式即可；可选的“按住修饰键临时平移”在原生键位验证后再增加，不预先占用空格、Alt 或 Ctrl。
- 必须在按下时确定本次手势归属，并接管完整的按下、移动、松开序列；不能仅在移动时画框而让原生代码同时准备或启动地图拖动。手势进行中不因修饰键变化切换归属。
- 本机原生 WorldInput.lua 的 OnMouseStart 调用 ReadyForDragMap，OnMouseMove 达到拖动阈值后调用 StartDragMap/UpdateDragMap；Expansion2 通过 Expansion1 包含基础脚本。应在有效 WorldInput 路由中分流，不能假设额外 UI 的监听器一定先于原生处理器。
- 区分点击、双击与拖框，阈值按 UI 缩放处理。框选期间暂停边缘滚屏；若无法可靠冻结镜头，镜头变化即取消本次框选，避免画面移动导致选错。
- 从单位旗帜、地块、UI 按钮开始拖动分别测试；城市面板、小地图、菜单等仍由对应原生 UI 处理，不能用全屏透明层无差别吞掉输入。
- Esc、窗口失焦、弹窗、回合结束或切换模式要清除拖动状态，防止鼠标松开后仍在框选或地图无法拖动。
- 输入阶段先只验证矩形、选中数量和镜头是否静止，不发单位命令；再验证关闭群控后原生操作恢复。P0 面板成功不等于此项通过。

鼠标框选按屏幕矩形判定，必须处理地图旋转、缩放、UI 缩放和战略视图。实施前验证本机实际可用的屏幕位置/世界锚点接口；不能凭其他游戏的 API 名称假定存在 `WorldToScreen`。需要把单位旗帜的点击、拖动和普通地图输入一起验证。

## 实现结构

```text
Mod 入口与状态显示
        ↓
输入适配 → 己方单位选择集合 → 目标分配/攻击筛选
                                     ↓
                              顺序命令队列
                                     ↓
                          原生单位命令与游戏事件
                                     ↓
                          更新结果、原因与诊断日志
```

拟定模块：

- `GroupCommander.modinfo`：稳定且唯一的 Mod ID、版本、文件清单、规则集与加载动作。
- `UI/GC_Status.xml` / `GC_Status.lua`：可见版本、初始化状态、自检按钮、计数及失败提示。
- `UI/GC_Selection.lua`：以 ownerID + unitID 维护集合；不只保存当前选中单位，也不长期持有失效对象。
- `UI/GC_Input.lua`：模式切换、框选、双击、地图目标与事件消费。
- `UI/GC_Planner.lua`：合法候选格、占位预约、单位与落点匹配。
- `UI/GC_Queue.lua`：提交、等待、核实、暂停、失败与取消。
- `UI/GC_Diagnostics.lua`：统一前缀、构建标识和阶段日志。
- 后续如需跨回合和存档恢复，再添加经验证的存档状态组件。

界面入口采用文明 VI 实际支持的 `InGameActions` → `AddUserInterfaces` → `Context=InGame`，由 XML 上下文加载对应 Lua；文件清单与需要导入的脚本明确登记。不能只把脚本列入 Files 就认为它会执行，也不能把需要 UI API 的代码当作普通 `AddGameplayScripts` 运行。

独立面板先验证成功。输入整合先验证独立上下文能否正确收到/消费所需事件；如需替换 WorldInput，则使用范围明确的适配脚本并保留 Expansion2 原有流程。其他 UI Mod 同样替换该上下文时，需要兼容适配；提高加载顺序并不能自动合并两套逻辑。不要直接覆盖游戏安装目录中的原文件。

## 批量移动：为单位分别安排合法位置

文明 VI 的同类占位限制决定了多个战斗单位通常不能以同一格为最终停留位置。

1. 将点击位置视为集结中心，根据部队数量按圈扩展候选格，并限制搜索数量。
2. 检查单位领域、已知地形、可达性、外交权限及已占用位置。普通移动不借用会自动攻击或宣战的组合行为。
3. 按实际占位类别预约落点，同一占位类别不重复分配。
4. 优先采用较短的合法路径，减少交叉；按释放通道的顺序执行，不能仅按直线距离判断一定可达。
5. 下发前重新检查目标和单位状态；收到事件后检查位置/行动力/操作状态，确认进展。
6. 遇到堵路允许有限次重排；无可行解时保留未完成单位并说明原因，不能无限重试。

初期只验证当回合可完成的简单集结，再支持多回合远距离行军。后者要处理原生路径被取消、下回合重评估、玩家手动接管、读档恢复及重复下令问题。不能把“发出一次 MOVE_TO”直接当作长期集结已经可靠。

### 移动力不同与途中停止（用户最新补充）

- 每个单位分别查询原生剩余移动力、可达格、路径及实际终点，不用全队统一步数，也不用几何距离替代道路、河流、丘陵、树林、控制区等移动成本。
- 区分最终集结落点与本回合停留格。本回合够不到最终落点时，后续行军功能从合法路径中选择当回合可停留的中间格，并对中间格也执行占位预约；不能只预约远处终点。
- 行军单位状态至少区分：已到达、本回合移动完成但未抵达、无移动力待命、路径阻塞、执行被拒绝、结果未知。移动力耗尽是正常结果，不应显示脚本异常、不阻塞全队、不循环重发。
- 中途意外耗尽移动力时检查实际位置和操作状态；在确认该次操作结束后处理下一单位。只有无法确认命令结果才使用超时保护。不能把“不在最终落点”一概当成失败，也不能用超时跳过掩盖残留的跨回合命令。
- 首个集结测试版仍只安排当回合可达的候选落点，不宣称已经支持中途推进。护送组合暂时跳过并说明原因。远距离推进完成后，下一回合默认由玩家点击继续，再重新计算，不自动续攻。
- 新增验收：同兵种不同地形成本、0 移动力混选、快慢单位混选、过河/道路/控制区、途中格竞争、实际停止点与预测不同、下一回合继续与手动取消。

### 联合测试方式（用户最新约定）

- 默认由用户操作游戏；助手集中修改代码、静态检查和读取短日志，不再反复接管鼠标、逐步截图。
- 每轮安装一个明确构建，给用户 3—5 个操作步骤及预期结果；用户完成后回复测试编号与结果，助手运行 tools/Read-TestReport.ps1 获取最近会话证据。
- 不高频轮询整份日志。仅在用户反馈完成或错误时读取；需要等待时不反复调用模型。画面可见性、拖动手感等由用户确认，日志不能替代这些体验验收。

禁止通过直接设置坐标、恢复移动力或放宽占位规则实现群体移动。这会改变用户要保留的游戏规则。

### 目标格及周边位置的具体约定（用户追问后细化）

- 点击的格子是集结中心。对同一占位类别，至多给一个单位预约该格，其他单位分配周边格。战斗、支援、平民等能否同格，交给原生占位规则判断，不能写死所有单位都互斥。
- 在无障碍、同类单位且位置均可用的示例中，10 个单位可分配为中心 1 个、相邻六格 6 个、第二圈 3 个。这只是几何示例，海岸、山地、第三方单位、路径和外交权限会减少可用格。
- 中心格不是固定给鼠标方向上“最靠前”的单位。优先比较可达性与路径成本，并避免占中心后堵住其他人的通道；必要时中心单位后走。若玩家显式指定领队占中心，则在合法时尊重该指定。
- 周边不足时在限定半径内扩展；仍无位置的单位保持当前位置，显示“无可用落点”。不能为了容纳全队无限扩大范围，也不能把单位自动挪到敌军另一侧或未授权的远处。
- 候选格优先考虑部队接近目标的一侧及连通区域。地形合法只说明可以进入，不代表战术上安全；界面预览落点和移动线路，供玩家判断。
- 被框选但未移动的单位也会占位；已预约但尚未到达的格子也会占位。不能在规划时把它们当作空地。允许根据实际完成的移动更新占位和有限重排。
- 默认不挪动未被选中的驻军，不拆开已有护送关系。受绑定单位先作为受约束组合处理，不能可靠规划时明确跳过。

### 混合单位、同格兼容与护送（用户再次追问后细化）

群控必须利用游戏允许的混合占位，不能把所有单位都强行摊到不同格，也不能把“兵种不同”误认为可以同格。

本机 `Base/Assets/Gameplay/Data/Units.xml` 已核对：

| 示例单位 | FormationClass 配置 | 对普通陆地落点的意义 |
| --- | --- | --- |
| 骑兵、弓箭手、勇士、长矛兵 | FORMATION_CLASS_LAND_COMBAT | 尽管战斗/晋升类别不同，仍竞争陆地战斗占位 |
| 建造者、开拓者、大将军、大科学家、大商人 | FORMATION_CLASS_CIVILIAN | 可以分别与陆地战斗单位同格，但这些平民彼此通常竞争同一位置 |
| 攻城锤 | FORMATION_CLASS_SUPPORT | 可以在原生规则允许时与战斗和平民单位同格 |

这些是配置证据，最终以当前对局合并后的数据、单位状态和原生移动检查为准。FormationClass 只用于初步分类，不是覆盖所有单位的完整占位判定：宗教单位、商路、海军、航空基地容量及其他 Mod 的特殊单位必须按实际机制单独处理。不应根据一个字段或“非战斗单位”标签推导无限叠放。

实现规则：

1. 选择界面的分类与占位分类分别维护。玩家可以按战斗单位、建造者、开拓者、伟人、支援等用途筛选；大将军在界面可列为伟人，但不能因此获得额外占位。双击同兵种按实际单位类型筛选，不按整个占位类别筛选。
2. 框选提供“仅战斗单位 / 全部单位 / 自定义筛选”。仅战斗模式在同格有骑兵和建造者时只选骑兵；全部模式应枚举格内全部己方单位，不能只选地图上最显眼的图标。选择列表展示每个成员，可单独移除。
3. 每个候选格记录完整的实际驻留单位与计划进入单位。按兼容性预约多个位置，而不是给整格设置一个“占用/空闲”布尔值。骑兵预约不一定阻止建造者预约，但建造者预约会阻止大科学家占用同一个普通平民位置。包含未选中的驻留单位，也必须考虑行进过程中临时停留的位置。
4. 规划以保留已有同格搭配和原生护送关系为优先，在合法、可达时让搭配抵达同格。即使两个单位目前站在一起，也不能假定它们已建立原生护送；“同格”“群控选择组”“原生护送”“军团/集团军”是不同关系。
5. 已有护送组合按完整成员检查路径、有效移动力与共同停留位置，避免重复发令导致拆散或多次移动。组合只能走到所有成员可合法停留的位置。若仅框选一个已链接成员，界面显示会随行的其他成员及实际影响；不能暗中拆链。无法可靠处理时跳过该组合并提示原因。
6. 未链接的同格单位保持搭配属于调度偏好，不保证中途永远受到保护。若需要可靠共同移动，应使用游戏原生护送并验证可建立；不能只把两个独立移动命令发向同一终点，就宣称提供了全程护送。新建/解除护送作为明确操作，不因一次群体移动自动改动。
7. 示例：3 个骑兵、2 个建造者、1 个大科学家，在可达且占位允许时可分到 3 格，每格一个骑兵搭配一个平民。若只有 1 个骑兵及这 3 个平民，普通规则下至少要给 3 个平民安排不同位置，不能让一个骑兵同时容纳三个平民。
8. 群体移动适用于被选中的可移动单位；群体攻击仅筛选具有对应合法攻击能力的成员。建造者、开拓者、大科学家、大将军等不会被当作攻击者，也不会自动消耗建造次数、建立城市、激活/退役伟人。支援/大将军跟随布阵作为明确选项，而非默认把所有平民推向敌人。
9. 攻击者处于原生护送组合时，执行前验证整组影响。不能未经验证把普通移动的“共同终点”规则直接套用到攻击、占城和撤退；无法可靠保证成员合法时提示该组合需单独处理。
10. 预览按格显示单位图标和数量，例如“骑兵 + 建造者”，并区分现有驻军与本次将进入的成员。同格允许性、路径与行动力必须在实际提交前再次检查，规划预约不能替代游戏最终判定。

新增验收：骑兵+建造者分别和联合选择；骑兵+大将军；骑兵+大科学家；骑兵+攻城锤+一个平民；两个不同平民争用同格；骑兵和弓箭手争用战斗位置；目标格已有未选中的平民；三骑兵三平民的混合集结；已有护送组合仅选中一员、移动力不足、路径变更；攻击时混选平民不触发其特殊行动。特殊规则单位需另列适配测试，不凭上述普通示例宣称全部支持。

## 批量攻击：逐次执行，逐次重验

先实现同一可见、已交战目标上的远程集中攻击，再支持近战和混合部队。

- 根据本机原生代码使用射程、行动力、剩余攻击次数及原生操作合法性检查。不能仅凭兵种类别判断可以攻击。
- 一次提交一项攻击，等待游戏状态推进后才考虑下一单位；快速战斗关闭或开启都必须测试。
- 每次执行前重新确认目标所有者与身份、位置、存活、视野及交战状态。
- 敌军死亡、撤离或城市易主后停止原目标攻击队列，避免剩余单位继续冲入该格。
- 近战攻击依照原生移动攻击参数实现，不凭空假定存在独立的 `MELEE_ATTACK` API。
- 普通群体攻击允许按下述约定在本回合内移动补位并攻击；只为接近目标而移动的行为归入独立的“推进围攻”模式。两者均不擅自换目标、追击失去视野的目标、跨回合续攻或自动宣战。
- 飞机、核武、宗教战斗和其他特殊行动后续单独适配。

### 射程不足与攻击位置

普通群体攻击采用以下处置，按实际状态动态调整：

| 单位当前状态 | 默认处置 |
| --- | --- |
| 当前可合法攻击目标 | 优先纳入攻击队列，避免先移动大量后排后目标已被消灭 |
| 当前不能攻击，但本回合可到合法攻击位，且移动后仍满足攻击条件 | 预约独立攻击位；轮到它且目标仍有效时才移动；移动结束重新验证，合法才攻击 |
| 能走近，但移动后不能攻击、没有剩余攻击次数或本回合到不了攻击位 | 保持当前位置和剩余行动力，显示原因 |
| 没有空攻击位、路径不通、被前排堵住 | 等待本批次产生可用位置后有限次重评估；仍不合法则待命 |

攻击位不是简单的“目标周围空格”：近战必须有合法的近战接敌路径，远程必须满足实际射程、射线/视野及能力条件。行动力消耗、地形、控制区、移动后能否攻击、单位能力或晋升都必须参与检查。单靠六角距离和单位名称不能证明能攻击。

当前找到的原生 CanStartOperation 用法以实际单位位置为依据，不能假定它支持假想落点的完整攻击预测。开发时需要验证路径结果和候选位置判定能力；若无法可靠预测移动后可攻击，普通群体攻击应将该单位标为待命，允许用户通过推进模式明确移动。移动后必须再次调用实际状态检查，不能靠预测强行攻击。

近战通常竞争目标周边相邻格，远程使用其射程内可合法开火的格子；给能够占领城市的单位保留可行接近路径。所有攻击位按占位规则唯一预约。第一版不为使更多单位攻击而自动把已在前排的单位撤走：是否轮换涉及伤害、行动力和阵地取舍，不能默认为无代价。

“推进围攻”作为明确可选的命令：允许原本够不到目标的单位向分配的攻击位置靠近，本回合只能走一段的单位走到合法停留格，标记为“接近中/本回合不能攻击”。它不能绕过格子容量，也不保证本回合所有单位都能攻击。该模式依然先使用可立即攻击的单位；需要移动的单位在轮到时才行动，并在每个步骤前检查目标。跨回合的继续推进/攻击须另行明确设计；首版回合结束暂停，下一回合由玩家点继续。

### 目标消失、城市占领后的剩余单位

- 命令绑定目标身份（目标类型、所有者、ID、原位置及本批次 ID），不能只保存目标格坐标。目标死亡、移动、失去可见性、易主、消失或已不再交战时，停止该批次后续攻击和接近动作，状态分别标为完成或中止。
- 城市不能按“城墙被打掉”或“血条看起来见底”判断已完成；以城市实际易主/被占领或移除为完成条件。若仍属敌方而没有合法占城单位，显示“需要可占城单位完成占领”，保留未使用单位的行动力。
- 尚未开始行动的单位留在原地；已经移动的单位停在实际到达的位置并保留剩余行动力；完成近战占格/占城的单位遵循原生结果。Mod 不恢复已消耗行动力，不撤销已结算伤害。
- 立即作废本批次尚未提交的动作。对于原生命令中已经提交的移动，只能使用经实测的取消方式阻止后续路径；正在结算的战斗或已开始的一步移动不能承诺瞬间撤销。这也是采用逐步下令、限制同时在途动作的原因。
- 取消只能作用于本 Mod 当前批次仍拥有的命令；如果玩家已手动接管，不得取消玩家的新命令。遇到占城后的保留/夷平弹窗，先暂停调度并让玩家处理。
- 不自动换到另一个敌人、不自动拥入目标格、不自动把剩余单位设为睡眠/跳过回合。保留群组选中状态，显示“目标已消灭，X 个单位尚可行动”，可提供“只选未行动单位”按钮以便下一个目标。
- 下一回合、读档或过期事件回调不得复活已经完成/取消的攻击批次。

### 新增验收场景

1. 10 个同类战斗单位点一个空格：落点没有非法重叠，中心和六格不够时使用合法的外圈位置。
2. 沿海/山地/狭路集结：空间不足者有明确待命原因；不跨到隔绝区域，不驱逐未选驻军。
3. 后排远程虽在几何射程内但被地形阻挡：不能发送无效攻击；有合法补位才考虑移动。
4. 单位可到攻击位但移动后不能攻击：普通攻击不擅自移动；推进模式按其明确含义处理。
5. 前排占满相邻格且无法让路：后排待命，不瞬移、不自动强制轮换、不无限重试。
6. 首次攻击就消灭目标：其余单位位置、剩余行动力和攻击次数不因本批次改变。
7. 部分单位移动后目标被消灭：未提交的补位撤销，已经发生的行动不回滚；检查无残留自动路径。
8. 城墙消失但城市未易主：不能误报占城完成；城市真正易主后剩余攻击停止。
9. 同一格后来出现另一敌方单位、目标逃走或玩家手动接管：旧批次不能继续攻击或覆盖玩家命令。

## 最高优先级：加载验证与故障定位

第一个开发版本只承担“证明整条加载链路有效”的任务。必须在进入新对局地图后显示：

```text
群控 Mod v0.1 | 构建标识 | UI 已加载
本地玩家：已就绪 | 自检按钮：可点击
```

游戏运行时再记录后续阶段。以下是拟定日志格式，当前还没有生成这些运行证据：

```text
[GC][BOOT] build=...
[GC][UI_READY] context=InGame
[GC][PLAYER_READY] player=...
[GC][INPUT] action=...
[GC][SELECT] count=...
[GC][ORDER_CHECK] unit=... reason=...
[GC][ORDER_SENT] order=...
[GC][ORDER_RESULT] order=... state=...
```

| 表现 | 应先检查的证据 |
| --- | --- |
| 附加内容里看不到 Mod | 实际扫描路径、解压目录层级、扩展名、modinfo XML、文件清单 |
| 能启用，地图内没有版本面板 | 游戏内加载动作、规则集条件、XML/Lua 关联、脚本初始化错误 |
| 面板可见，自检按钮没反应 | 控件绑定、遮挡与 UI 输入处理 |
| 按钮正常，地图框选没反应 | 地图输入适配是否加载、扩展替换、单位旗帜事件、模式和事件传播 |
| 显示选中，命令没有提交 | 本地回合、单位所有权、对象有效性、UI 上下文、操作检查失败原因 |
| 已提交，单位仍未移动 | 原生拒绝/取消、目标占位、路径、行动力、异步状态未更新 |
| 改代码后表现完全没变 | 面板构建标识、重复 ID、旧安装副本、游戏是否仍加载旧上下文 |

Modding.log 用于加载动作，Lua.log 用于脚本运行，UserInterface.log 辅助界面资源问题，Database.log 用于实际涉及数据库的组件。只能把与本 Mod ID、文件名和测试时间相关的错误归因于本 Mod，已有的无关警告不能作为旧 Mod 失败结论。

每次验证保留游戏版本、规则集、Mod 构建标识和本次日志。首次加载测试用明确启用新 Mod 的小地图新对局；新对局通过后再验证现有存档，不能先假定 UI Mod 随时加进任何存档都有效。修改 modinfo 或新增文件后，首选完整重启，排除旧上下文和缓存干扰。

## 阶段与通过条件

| 阶段 | 交付 | 必须看到的游戏内证据 |
| --- | --- | --- |
| P0 加载探针 | 唯一 Mod、可见版本、自检按钮、分层日志 | 附加内容识别；地图内面板出现；点击可计数；重启仍可复现 |
| P1 单兵命令 | 用面板指定一个己方单位并发原生命令 | 单位按规则移动；验证非原生当前选中单位也能接收命令 |
| P2 最小群体移动 | 多选列表、三单位合法落点、顺序队列 | 一次操作让三单位分别移动到不同合法格；失败有原因 |
| P3 铁锈式选取 | 屏幕框选、双击同类、Shift 增减、右键下令 | 选择数量正确；退出模式后原生地图、城市、单位操作正常 |
| P4 群体攻击 | 远程，随后近战与混合部队 | 攻击顺序合理；目标死亡/城市易主后停止；不重复攻击 |
| P5 后期可用性 | 30/100 单位、跨回合、存读档、特殊地形 | 不无限等待/重复发令；可取消；记录真实耗时与未完成原因 |

所有阶段都须先完成上一阶段的游戏内验收。静态检查通过只代表文件结构与部分逻辑通过检查，不代表游戏成功加载。

P0/P1 是决定继续扩展的首要关卡。若事件接入或非当前选中单位命令请求受限，先用这两个阶段暴露限制，调整适配设计，再进入复杂群体调度。

## 测试范围与交付

- 普通平地、狭窄通道、占用目标、河流、山地/不可达位置、陆海混合、零行动力、护送绑定。
- 单位死亡、升级、编队身份变化、玩家手动下令、回合结束、加载存档。
- 打开城市面板、外交/弹窗、界面缩放、地图缩放和旋转、战略视图、单位旗帜点击。
- 执行队列每次重新读取单位；回合结束暂停；取消/卸载时移除事件监听，避免初始化多次后重复发令。
- 性能采用事件驱动与限定候选范围；不在每帧扫描整张地图，不进行大量全图寻路。记录实测响应时间再优化。

最终交付应包括安装目录清晰的 Mod 包、中文操作说明、版本标识、最小复现场景、日志检查方法和已实测兼容范围。安装工具只管理本 Mod 的唯一目录，检查两处用户目录及重复 ID，保留恢复方式；不能为了排障批量删除其他 Mod、存档或游戏配置。

## 外部参考资料

- 铁锈战争开发者介绍：<https://corrodinggames.com/rusted_warfare>。用于核实其 RTS 与大规模单位战斗定位。
- 文明 VI 官方手册：<https://downloads.2kgames.com/civilization/vi/manuals/eu/CIV_VI_25TH_ONLINE_MANUAL_ENG.pdf>。用于移动、占位及编队规则；当前版本细节最终以本机游戏为准。
- 作者的最小 Lua Mod 示例：<https://jonathanturnock.github.io/civ-vi-modding/docs/hello-world-lua-mod/>。用于理解加载动作和 Lua 日志验证；其旧 Documents 路径不能直接套到本机，其 GameplayScript 示例也不能替代 UI 上下文入口。
- CQUI Lite 的 modinfo 源文件页面：<https://github.com/EilRoviSoft/Civ6-CQUI-Lite/blob/main/CQUI.modinfo>。可继续参考真实 UI Mod 的组织方式，实际实施优先对照本机官方 DLC。

搜索结果中的 Civ V / Vox Populi 的 Squads 和群体移动 Mod 不属于文明 VI，不能把它们当作本任务已在文明 VI 实现的证据。
