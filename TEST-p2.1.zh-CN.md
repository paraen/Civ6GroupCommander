# p2.1 移动测试：由玩家操作

构建号必须为 **20260921-p2.1**。本版已加入实际 MOVE_TO 请求，但尚待本机游戏验收。不是最终完整版。

## 第一轮（约两分钟）

1. 安装完成后完整退出并重新启动文明六，加载一个单人风云变幻测试存档。左上角应显示“移动测试”和 `20260921-p2.1`，群控面板应有“执行本回合集结”。若仍是 p1.3，先不要重复测试移动。
2. 开启群控，框选一个有移动力、没有链接护送的陆地战斗单位。右键它附近本回合能到的空陆地（先用一两格距离）。应显示黄色落点编号及“可移动 1”。这一步只是预览。
3. 点击“执行本回合集结”。预期单位正常走过去、正常消耗移动力；面板最终显示到达 1。不要依赖原生当前选中的单位，观察被框选的单位是否行动。
4. 再用两个都有移动力的战斗单位重复：相同占位类别应分配不同落点。随后可切换“全部陆地”，测试战斗单位与开拓者/建造者；游戏允许时可以共用一格，不会建城或消耗建造次数。
5. 混选一个移动力为 0 的单位和一个还能移动的单位，再对附近空地预览执行。0 移动力单位应待命；其他单位仍可行动。返回对话告诉我：版本号、哪一步、显示的可移动/到达/待命数量。

先完成第 1—3 步即可反馈，不用为凑齐各种兵种开新局。日志我直接读取，你不需要手工复制整份日志或截图。

## 当前明确限制

- 只处理本回合可达的可见陆地，目标周围最多四格内找落点；不是远距离自动行军。
- 每批最多 64 单位。护送队、商人/间谍、非陆地及不支持的占位类型暂待命，原因记录在状态提示和日志。
- 不进入其他玩家领土或穿越其单位，不使用自动攻击/宣战/换位辅助函数。不包含群体攻击。
- 移动力耗尽时检查实际位置；清理残留路线得到确认后继续其他单位。若引擎结果无法确认，则停止批次并提示，不无限重试。
- Esc、关闭群控或重新选择会取消尚未发出的命令，并尝试取消当前路线；已经发生的移动不会回滚。

## 助手读取的证据

运行 tools/Read-TestReport.ps1。关键日志：RALLY_PLAN、RALLY_PLAN_UNIT、RALLY_REQUEST、RALLY_ARRIVED、RALLY_STOPPED、RALLY_WAIT、RALLY_COMPLETE；异常看 SELECTION_ERROR / ERROR / RALLY_CLEAR_ERROR。

14 项离线测试已通过，使用的是模拟引擎接口；不能据此认定文明六原生请求已验收。
