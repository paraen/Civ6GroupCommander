# 接续此项目先读这里

## 最新：20260923-p2.7 已安装，等待自动跳过本回合实测

- 安装时游戏未运行，Documents Mods 下13个文件与源码SHA256一致。

- 本轮用户指出剩余移动力不足跨越下一格时，原生要求手动跳过。日志2026-09-23 08:52:11确认p2.6行军/追击跨回合保留，也有手动接管导致PURSUIT_RELEASE；MARCH_SAVED empty=true证实显式清空已传到Gameplay，真实读档仍不能据此宣称通过。
- p2.7在行军/进攻本回合队列结束后，对仍有任务/未到达/剩余移动力大于0且没有合法继续落点的单位，调用原生SKIP_TURN一次，保留任务下回合重查。只处理清醒、自己的活动回合；游戏忙/暂停/原生拒绝时不提交。不使用驻守、睡眠或强改移动力。操作事件不当作手动接管。
- 原生依据：Base/Assets/Gameplay/Data/UnitOperations.xml的UNITOPERATION_SKIP_TURN，UnitPanel.lua的RequestOperation(actionHash)执行方式。
- 回归通过：17集结、15分类、28攻击、35行军、26战术，加既有输入/菜单/存档桥、11Lua语法与XML检查。新增测试包括剩余1点跨不过2点地形后自动等待/次回合继续、到达不跳过、手动接管不跳过、有合法路不跳过、原生拒绝不丢任务、协同等待不释放。备份GC-p2.6-before-turn-wait.zip。
- 用户按TEST-p2.7.zh-CN.md实测提示是否消失，助手读日志TASK_WAIT_TURN与次回合续行。

## 最新：20260923-p2.6 已安装，等待自动交战与协同进攻实测

- 2026-09-23安装完成：游戏未运行，Documents Mods下13个文件与源码SHA256全部一致。

- 用户本轮授权闲置单位优势/均势自动攻击、劣势自动避让，以及群体远距离攻击的落点/优先级与连续推进优化。
- 新GC_Auto：默认关闭，独立开关，陆地清醒战斗单位；原生SimulateAttackInto强度修正/伤害/血量余量判断，预测不明待命。当前选中、框选手势、手动下令、驻守/睡眠、链接护送不接管。有任何群体行军/协同任务则自动模块整体让行。只原地合法攻击，不自动追敌；避让路径与终点需更安全，无安全格待命。每单位每回合一次，超时关自动。原生接口依据本机UnitPanel.lua的GetCombatResults和战斗预估展示代码。
- 新GC_Pursuit：用户明确确认的攻击任务按同一owner/id跨回合重规划，只跟随当前可见位置；目标死亡/视野丢失/外交改变停止。手动成员退出，动作未知超时停止任务，三回合无推进且无直接攻击安排交回控制。暂不跨存档恢复协同进攻。
- GC_Attack：最慢成员原生可达前沿约束快速成员最多领先约两格（已有攻击机会优先），候选落点少者先执行，执行优先使用匹配落点，初始被堵成员在前队走完后补查一次。零移动力队伍可接受下回合任务。自动攻击执行前再次检查手动状态和原生预测，绝不改成接敌移动。
- 上轮p2.5实测加载、筛选、跨回合移动及5单位接敌成功，但保存清空没有确认。此次改显式action=clear，不依赖空字符串；存档使用GC_MarchV2/version2/GC_SaveMarchV2，隔离可能残留的p2.5记录，升级后需重新指定行军目标。真实读档仍未实测。
- 本地验证：17集结、15分类、28攻击、30行军、25战术场景，加菜单/双击/输入/存档桥，通过11Lua语法与XML/manifest检查。新增算法尚未实机验收。备份GC-p2.5-before-tactics.zip。
- 测试说明TEST-p2.6.zh-CN.md；下一步用户测试，助手只读日志。不要声称支持海空完整移动、城市攻击、护送、联机或进攻存档恢复。


## 最新：20260923-p2.5 已安装，等待控制台与读档实测

- 2026-09-23 安装完成：游戏未运行，Documents Mods 下全部11个文件与工作区SHA256一致，含新增Gameplay存档脚本。

- 本轮用户要求代码优化、控制台清楚分层、突出侦察兵，并同意统一退出群控行为及读档恢复。旧p2.4实测日志2026-09-23 08:01:46已确认接敌后攻击与第36至39回合四单位全员抵达，不要再按旧记录认为跨回合未通过。
- p2.5控制台三段分区，诊断收起；两列分层目录、完整路径、单独应用父类按钮，浏览父类不改变筛选。侦察原本已有，现明确标名并加侦察兵/游骑兵分类测试；医疗兵与军事工程师归支援。
- 关闭群控/Esc/清除选择/筛选/预览不取消已执行命令，停止按钮统一取消。回合结束、错误、玩家变化、外交/全屏暂停仍处理攻击停止。执行新行军取消旧攻击，执行攻击停止旧行军；手动原生命令接管攻击批次。选择摘要仅变化时刷新与日志。
- 新增Scripts/GC_Save.lua游戏存档属性通路，官方本机BlackDeathScenario_StateUtils.lua提供UI.RequestPlayerOperation(EXECUTE_SCRIPT, OnStart)依据。UI只请求，Gameplay写玩家GC_MarchV1属性。快照保存目标/单位类型/位置/阻塞计数/最后下令回合，不保存攻击。读档验证归属与状态、拒绝重复本回合下令，释放存档时未确认在途单位。退出不擦除存档任务。
- 本地测试：17集结、15分类、28攻击、30行军，加菜单/退出群控/双击/原生输入/存档桥验证通过；9个Lua语法、XML/manifest检查通过。尚未实机验证新布局、新增Gameplay加载与真实读档。备份GC-p2.4-before-console.zip。
- 下一步用户按TEST-p2.5.zh-CN.md实测，助手读日志。不能把海空/城市/护送/跨回合追击说成已实现。

## 以下为历史交接记录

## 最新：20260921-p2.4 已安装，等待接敌与行军实测

- 2026-09-23 完成中断后的最终验证与安装：17集结、13分类、27攻击、24行军场景及回调、语法、XML检查全部通过。安装时游戏未运行，Documents Mods 下十个文件与工作区 SHA256 全部一致。新行为仍待用户实机验收。

- 用户本轮明确反馈：混合编组对敌军不能有效执行，贴脸近战可打、远处战斗单位不接敌；双击同兵种能选择并操控，已实机确认通过。
- 读取最新 Lua.log（更新时间2026-09-21 23:22:33）：多条 ATTACK_WAIT 近战须先移动到目标相邻格，orders=0 的批次没有可行动单位。另有混合编组实际攻击成功的记录，平民待命没有阻塞当时已经能攻击的单位；缺失的是移动接敌。
- 日志还发现 MARCH_REQUEST 后紧跟 MARCH_RELEASE reason=收到其他命令（多名单位重复）。旧逻辑把 UnitOperationAdded 单个/重复/异步通知当作新玩家命令，会错误取消行军。因此跨回合行为仍未通过实机验收。
- p2.4 新增本回合接敌：立即攻击优先，然后各陆地战斗单位按原生可达格/完整路径分配不同接敌落点，移动后重新检查攻击。不能本回合攻击者可推进后停步；非战斗单位原地待命。目标变化全批停止。不是自动跨回合追杀；海空接敌/城市/护送仍未适配。
- 攻击预览显示直接攻击/接敌/待命，数字展示接敌落点；执行按钮“执行推进与攻击”。“停止行军／攻击”按钮统一停止队列。零可执行攻击预览不再误取消已有行军。
- 行军不再因 UnitOperationAdded 通知直接退出，改为诊断 MARCH_OPERATION_EVENT；WorldInput 在原生手动右键或目标点击之前发 GC_ManualOrder 明确释放该单位，原有 UnitCommandStarted/位置变化接管保护保留。
- 本地验证通过：17集结、13分类、27攻击、24行军场景，加菜单/双击/原生输入回调、8Lua语法及XML/manifest检查。己方单位移除不再直接取消整个进攻队列，存活单位仍处理；消失单位不虚报攻击成功。新接敌和行军修复未实机验收。备份 GC-p2.3-before-approach.zip。
- 下一步由用户按 TEST-p2.4.zh-CN.md 测混合编组“一个近、一个远”的接敌攻击，再测不重新框选的跨回合续走；助手读日志，不自动接管鼠标。

## 最新：20260921-p2.3 已安装，等待跨回合/迷雾/双击实测

- 安装完成：游戏进程未运行，Documents Mods 下十个文件与工作区 SHA256 一致。用户下一步重启游戏确认 p2.3，先测双击多名同兵种，再测不重新框选的下一回合自动续走。

- 用户补充核心需求：一次指定远处目标后跨回合自动继续，允许对未探索/已探索但不可见的迷雾区域下令。新增 GC_March.lua。最终目的地与每回合中间落点分离；原生 GetMoveToPathEx 的 plots/turns（首回合为1）及 GetReachableMovement 决定当回合可达性，逐个提交并确认，最终和中间占位都按类别分配。
- 不可见格不查询 IsWater/IsImpassable/GetOwner/驻军，只使用几何位置及原生寻路结果。MOVE_TO 使用 MOVE_IGNORE_UNEXPLORED_DESTINATION，不加 ATTACK；途中出现可见外国领土/单位等阻碍则待命，不自动开战。三回合连续受阻释放并提示。
- 新按钮“开始持续行军”“停止持续行军”，独立行军状态行。LocalPlayerTurnEnd 暂停而不删除任务，下一次 LocalPlayerTurnBegin 延迟后重查并继续。关闭群控只退出选取；群控内 Esc 或停止按钮取消。手动操作事件/位置变化释放相应单位；结果未知超时停止，不重试。
- 任务当前保存在 UI 对局会话内；退出/读档/热重载会清除任务。不能声称支持存档恢复。当前一次一组、最多64单位，新任务替换上一组；执行攻击停止行军。海空行军/护送仍未适配。
- 用户询问双击何时实现：既有代码已支持双击屏幕内同 UnitType、Shift追加；本轮补了地图与旗帜入口、屏幕外排除、不同类型排除、Shift增减、双击尾随单击不覆盖的模拟回归。不是新增尚未实现的功能，但多名同类实机选择仍须验收。全图同类不默认开启。
- 本轮测试：新增23项行军模拟场景，原有17项集结、13项分类、14项攻击，以及选取菜单/双击/回合结束事件、旗帜回调检查通过。修过测试脚本缺失 end 后最终全绿。8个Lua文件语法与XML/manifest检查通过。新行军还没有游戏实测。
- p2.2备份 GC-p2.2-before-march.zip，保留p2.2筛选和攻击功能。按 TEST-p2.3.zh-CN.md 由玩家测试，助手读取日志，不接管鼠标。重点验证换回合后 MARCH_TURN/MARCH_REQUEST、迷雾寻路与取消没有残留路线。

## 最新：20260921-p2.2 已安装，等待本轮游戏实测

- 安装完成：Documents Mods 下九个文件与工作区哈希全部一致；安装时游戏未运行。旧版备份 GC-p2.1-before-filters.zip。
- 最终检查通过：17 项集结场景、13 项分类场景、14 项攻击场景，以及筛选菜单初始化/回调、7 个 Lua 语法与 XML/manifest 检查。未运行游戏验收新菜单及原生攻击。

- 本轮用户明确要求完整分层兵种筛选，并选择“默认攻击敌人；友军占用格则在附近集结”。新增 GC_Filters.lua、GC_Attack.lua，已在 manifest 注册。保留右键预览、按钮执行。
- 筛选：全部、全部战斗、陆地细类（含轻重骑兵、医疗/支援等）、海军四类、空军两类、非战斗各类、九类伟人。建造者/开拓者有首页快捷按钮。默认全部单位；特色战斗单位按 PromotionClass 归类，特殊平民支持 UnitReplaces。
- 集结中心与实际落点分开判定；占用中心允许寻找附近合法位置，实际落点和路线仍严查占位、可达性和外交。
- 攻击仅对已交战、可见的城外敌军单位；锁定 owner/id/位置，远程优先，相邻近战随后，逐个下令并复核。目标改变即停，不自动宣战；平民/支援待命。不包含移动后补打、城市攻击、跨回合行军。海空筛选不等于海空集结已实现。原生攻击行为与菜单布局尚待实机验收。
- p2.1 真实日志已验收：混合单位同格四轮成功；零移动力待命不阻塞其他单位；同类战斗单位分到不同合法格；后续三单位、四单位集结成功。17:54:54 更新的日志有 RALLY_CANCEL reason=mode_change requested=2 arrived=1，证明执行期间取消过，但没有足够证据证明下一回合无残留路线。
- 旧 README/PLAN/本文下方的“p1.3 未安装移动”“等待首次实测”等均为历史状态，不要再按旧状态重启开发。
- 旧版源码备份：GC-p2.1-before-filters.zip。新增 tests/filter_cases.lua、attack_cases.lua、selection_cases.lua，扩充 rally_cases.lua。运行 tests/run_tests.py。
- 延续用户操作游戏、助手读取日志的方式，不默认接管鼠标。下一轮按 TEST-p2.2.zh-CN.md 检查筛选菜单、友军占位、相邻近战/射程内远程攻击。

项目：文明 VI 的铁锈式群体控制 Mod。工作目录：`C:\Adaima\Civ6GroupCommander`。

## 当前交付：20260921-p2.1 已安装，等待用户第一轮游戏实测

- 已完成移动原型修正并安装到 Documents 下的 GC_GroupCommander，七个文件哈希一致。p1.3 备份：GC-p1.3-before-movement.zip。不要再告诉用户移动代码尚未安装。
- 已通过 tests/run_tests.py：五个 Lua 文件语法、manifest/XML、控件引用、14 项模拟引擎测试。测试库 lupa 2.8 位于 .test-deps，仅开发使用，不随 Mod 发布；受权限限制可能需提权运行。
- 当前操作：开启群控→框选→右键预览附近落点→点击“执行本回合集结”。真正发 MOVE_TO，逐单位检查与执行。游戏本身尚未验收移动，明确等待用户反馈。
- 处理：同类分格、跨占位类别共享、逐单位原生可达格/路径、0 移动力待命、途中耗尽检查并取消残留路线、收到清理确认后继续、超时停止而不重试、取消尚未发出的请求。
- 限制：只支持当回合可达的可见陆地，拒绝外国领土/单位路径；集结半径 4，每批 64；护送与特殊移动单位暂待命；不含攻击及跨回合行军。
- 第一轮让用户完整重启，确认 p2.1 和执行按钮，先一个单位附近空格测试，再两个单位。详见 TEST-p2.1.zh-CN.md。用户操作，助手读取 tools/Read-TestReport.ps1；不默认鼠标自动化。
- 下一次：优先读取用户结果及最新日志，修复实机暴露的首个问题。重点确认原生 API、操作结束/清理事件的时序以及未作为原生当前选中单位也可接收移动。模拟测试不能代替此验收。

## 先前断点（已被上方 p2.1 交付更新）

- 用户亲测确认：开启群控后拖动变成框选，不再拖地图；选中后无法移动。已核对 Documents 实际仍安装 p1.3，只包含五个 UI 文件，没有 GC_Rally.lua；这是尚未安装移动功能，不是用户操作错误。
- p1.3 后续已实测：旗帜单击选入群组、双击事件接通、右键只预览；修复双击之后再次单击覆盖同类选择，日志 FLAG_DOUBLE_RELEASE consumed 已确认。混合框选 count=2 回归通过。尚未验证多名同类、Shift 手势、所有关闭模式原生操作。
- 工作区已有 GC_Rally.lua 及对应 manifest/XML/Selection 集成，但未安装、未实机验收。实现了当回合可达候选、占位类别匹配、预览编号、执行按钮、逐个 MOVE_TO NONE 请求与位置回查。不能将其称为已完成的移动版。
- 移动原型仍须修整：阶段/构建文字仍 p1.3；UI 提示部分仍说仅预览；正常中途耗尽移动力不能都归类为 15 秒超时。先落实用户补充的状态判定，再静态检查和安装明确 p2 构建。现阶段只支持当回合集结，不应冒充远距离推进；护送暂跳过，64 单位上限。
- 用户新增要求：每单位按原生地形和路径成本计算移动，耗尽移动力正常待命；远程行军区分最终目标与当回合停留格，并对中间格预约占位。已补入 PLAN。
- 测试方式改为：用户操控游戏，助手写代码和读取精简日志。不要默认接管鼠标或反复截图；每轮给 3—5 个步骤，用户反馈后读日志。不高频轮询模型。工具 tools/Read-TestReport.ps1 已添加，用于过滤最近加载会话的 GC/错误日志。
- 下一次从完成移动原型检查、更新清晰构建号与安装开始；之后邀请用户进行单兵→两单位→混合单位→移动力不足的分批测试。不要再从 P0 或游戏菜单截图开始。

## 历史阶段记录（以下有过时状态）

- 用户已完成需求讨论，并询问是否可正式开始；当前已开始 P0 最小加载验证版。
- 工作区与安装目录构建 `20260920-p1.1`；源码位于 `mod/GC_GroupCommander`，五个文件哈希一致。P1 为选取原型，尚在实测中，不发送单位命令。
- p0.2 已实测修复中文，单击计数 1、2；保存并重载后恢复显示、计数回到 1。日志确认 SHUTDOWN 清理旧监听。P0 压缩归档为 `GC-P0-20260920-p0.2.zip`。
- P0 的目标：游戏内可见面板、自检点击计数、分阶段日志。P0 通过前不扩展到群体移动或攻击。

## 用户最重要的要求

以前多次遇到“Mod 可安装、可上传创意工坊，但游戏内完全不出现”。安装、发布、静态检查都不是验收。必须实机看到版本面板并能点击。

## 确认的环境与关键发现

- Windows + Steam + 扩展包；首版按单人风云变幻设计。旧 Mod 已删除。
- 安装：`C:\steam\steamapps\common\Sid Meier's Civilization VI`，Steam buildid 15296837。
- 已实测：同一份 P0 放 AppData Mods 未被识别，移到 Documents Mods 后列表出现且已启用。安装路径为 `C:\Users\31767\Documents\My Games\Sid Meier's Civilization VI\Mods\GC_GroupCommander`。AppData 的 P0 副本已移走；其他 Mod 未改动。日志仍在 AppData 的 Logs。
- 原生 Expansion2 `InGame.lua` 348–353 行以 `isHidden=true` 创建新增 UI，需要主动显示。
- UI 入口：`InGameActions` + `AddUserInterfaces` + `Context=InGame` + XML；同名 Lua；辅助文件导入。不能以 GameplayScript 替代 UI 上下文。
- Expansion2 会替换 WorldInput。地图输入和面板按钮须分开验收。
- 当前固定 Mod ID：`e77c36ce-6992-4d2b-a4e8-f0432196f805`。

## 已定行为

- 框选/双击同类/Shift 增减；默认双击限当前屏幕，全图同类为明确操作。
- 点击空地是集结中心；按可达性、占位与路径分配周边落点，空间不足者待命。
- 混合单位保留原生同格规则；骑兵与建造者可同格，但多个普通平民不能据此堆到同格。选择类别与占位类别分开。
- 已有护送不拆散；同格不等于已经链接护送。
- 群体攻击先当前能打，再可靠判定移动后能打的单位补位。只靠近不攻击属于明确的推进围攻模式。
- 目标死亡、移动/失去视野、城市易主后停止相应批次，不自动换目标；保留剩余行动力，不回滚已发生的行动。
- 平民混选不触发建城、建造、伟人消耗。首版不宣称全兵种/联机均支持。

## 后续阅读

1. `PLAN.zh-CN.md`：完整行为、架构、阶段和验收。
2. `AUDIT.zh-CN.md`：加载隐藏状态、上下文和接口风险，27 项风险及对策。
3. `README.zh-CN.md`：P0 的检查方式与日志。

新任务可使用：“继续 C:\Adaima\Civ6GroupCommander 的文明六群控 Mod。先读 HANDOFF.zh-CN.md、PLAN.zh-CN.md 和 AUDIT.zh-CN.md。从实际记录的进度继续；先完成 P0 实机加载验收，不要直接跳到群控功能。”

## 验证记录

- p0.1 包结构、XML 解析、控件引用与安装文件哈希检查通过。
- 新建单人风云变幻小地图，随机祖鲁，第 1 回合；游戏地图可见 P0 面板，本地玩家 0。开局顾问提示关闭后，点击自检使计数增加到 1。
- Lua.log 确认 Documents 入口、BOOT、UI_READY、SELF_TEST clicks=1 player=0 requestAPI=true checkAPI=true commandsIssued=0。接口存在不能证明单位命令执行成功。
- p0.2 上述检查现已通过。专用保存档 `GC-P0-20260920-Test`，单人风云变幻、小地图、武则天、第 1 回合，未下单位命令。未覆盖用户其他档。
- 用户恢复任务时说已打开文明六。需要通过 computer-use 技能重新观察当前窗口，不能假设仍停在先前测试局，不应关闭用户现有对局。
- 用户新指出左键拖地图冲突；已补充 PLAN 的手势分流方案。P1 已写入并安装 GC_WorldInput.lua（包含原生 Expansion2，包装输入路由）及 GC_Selection.lua（模式开关、矩形、陆地筛选、世界锚点标记、Shift 增减）。实机验收尚在进行，不能声称已验证。
- P1 安装后退出并正在重启游戏；旧窗口句柄不可复用。用 computer-use 重新发现当前窗口。当前 P1 不包含移动与攻击，右键仅预览目标格。

## 2026-09-21 输入实测接续（p1.3 安装后）

- p1.1 实机框选：默认筛选框住勇士和开拓者时只选勇士 count=1；切换全部陆地后同框 count=2，两者青色菱形显示，镜头未拖走。
- p1.2 已安装且启动进入专用测试局，标题和阶段文字已从 P0 更新为 P1。Esc 实测退出群控且未打开原生菜单。
- 普通拖地图未标通过：自动化 sky.drag 只产生一个 MouseMove，日志 `NATIVE_DRAG moves=1 cameraDelta=0,0`。原生 WorldInput 第一个移动事件只 StartDragMap，后续才 UpdateDragMap。因此不能用该自动化手势判定原生拖动损坏；真实鼠标连续拖动待验证。前六次原生拖动有诊断日志。
- p1.2 加 hover 识别仍未解决旗帜点击：实测旗帜按钮抢先接收，原生勇士被选中但群选 count=0。这是独立于 WorldInput 的入口。
- 已编写并安装 p1.3：新增 GC_UnitFlags.lua，包装原生 UnitFlag.SetInteractivity，分别处理左击/双击/右击；开启群控时走 GC_FlagSelect/OrderPreview，关闭时保留原生单击。面板启用群控要求地图和旗帜两个握手均就绪。清理退出监听。蛮族氏族配置开启时保留原生氏族 include 链，尚未实测此模式。
- p1.2/p1.3 另修：Esc 按下退出后吞掉对应释放事件；失去焦点退出群控；选取异常关闭群控；单位移除时清除选择；Shift+双击为追加同类。
- 当前六个源码与 Documents 安装文件哈希相同，XML/manifest 解析通过。p1.3 新旗帜入口尚待完整重启后验收。复制 Lua 时游戏会自动热加载，但新 manifest 替换入口仍需重启。
- 当前操作：已 Alt+F4 并确认退出专用测试局，等待退出后重新启动。窗口需重新发现，不能复用句柄。
- 下一步：完整重启→加载 GC-P0-20260920-Test→确认 FLAGS_BOOT/FLAGS_CONNECTED→实测勇士旗帜单击、双击、右击、混合框选、Esc 和关闭模式原生点击；若失败先读 Lua.log 首个错误。随后推进 P2 落点分配/移动。尚未实现任何群体移动或攻击，也未向单位下令。
- README 已更新 P1，但构建号暂仍 p1.2，完成本轮后同步 p1.3 实测结果；本文开头与旧“新任务提示”存在过时 P0 状态，应以本节为准。
